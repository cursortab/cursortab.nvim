Completion populating an empty buffer (single empty line) with multiple new lines.
row: 1
col: 0
viewportTop: 1
viewportBottom: 20
-- buffer.txt --

-- steps --
completion 1-1
  | "package main"
  | ""
  | "func main() {}"
expect shown
  buffer:
    | "package main"
    | ""
    | "func main() {}"

