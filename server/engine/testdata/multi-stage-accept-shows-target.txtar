After accepting the first stage, the engine transitions to hasCursorTarget pointing at the next stage when it's beyond proximity threshold.
row: 2
col: 0
viewportTop: 1
viewportBottom: 20
-- buffer.txt --
function a() {
  return 1;
}

function b() {
  return 2;
}
-- steps --
completion 1-7
  | "function a() {"
  | "  return 10;"
  | "}"
  | ""
  | "function b() {"
  | "  return 20;"
  | "}"
expect shown stageCount=2

accept
expect state=hasCursorTarget cursorTargetLine=6

