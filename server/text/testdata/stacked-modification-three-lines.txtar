cursorRow: 1
cursorCol: 0
viewportTop: 1
viewportBottom: 50
availableWidth: 60
-- old.txt --
const aaa = oldImplementation(p1, p2, p3);
const bbb = oldImplementation(q1, q2, q3);
const ccc = oldImplementation(r1, r2, r3);
-- new.txt --
const aaa = brandNewImplementation(p1, p2, p3, p4);
const bbb = brandNewImplementation(q1, q2, q3, q4);
const ccc = brandNewImplementation(r1, r2, r3, r4);
-- expected --
stage @1 cursor=3:51
  modification @1 1-3 stacked 0:0
    + "const aaa = brandNewImplementation(p1, p2, p3, p4);"
    + "const bbb = brandNewImplementation(q1, q2, q3, q4);"
    + "const ccc = brandNewImplementation(r1, r2, r3, r4);"
    - "const aaa = oldImplementation(p1, p2, p3);"
    - "const bbb = oldImplementation(q1, q2, q3);"
    - "const ccc = oldImplementation(r1, r2, r3);"

